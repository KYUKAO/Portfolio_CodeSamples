using System.Collections;
using System.Collections.Generic;
using HackMan_GD07;
using UnityEngine;

public class VoidComponent : BaseGridObject
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
